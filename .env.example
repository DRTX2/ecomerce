# =====================================================
# ARCHIVO DE VARIABLES DE ENTORNO - DESARROLLO
# =====================================================
# Copia este archivo como .env para desarrollo local
# El perfil 'dev' se activa automáticamente si no se especifica
# =====================================================

# ============================
# PERFIL ACTIVO
# ============================
# Valores posibles: dev (desarrollo) | prod (producción)
# Si no se especifica, usa 'dev' por defecto
SPRING_PROFILES_ACTIVE=dev

# ============================
# BASE DE DATOS
# ============================
DB_URL=jdbc:postgresql://localhost:5432/amazon_db
DB_USERNAME=postgres
DB_PASSWORD=root

# ============================
# JWT / SEGURIDAD
# ============================
# IMPORTANTE: Cambiar en producción por una clave segura de al menos 256 bits
# Genera una con: openssl rand -base64 32
JWT_SECRET_KEY=my-super-secret-key-for-development-only-change-in-production-please

# Tiempos de expiración de tokens (en milisegundos)
# Los valores por defecto se definen en application-dev.yml y application-prod.yml
# Solo necesitas configurar estos si quieres sobrescribir los defaults

# Access Token (opcional - usa default del perfil activo):
# - Dev: 24 horas (86400000)
# - Prod: 15 minutos (900000)
# JWT_ACCESS_EXPIRATION=86400000

# Refresh Token (opcional - usa default del perfil activo):
# - Dev: 7 días (604800000)
# - Prod: 7 días (604800000)
# JWT_REFRESH_EXPIRATION=604800000

# ============================
# CLEANUP DE TOKENS
# ============================
# Los valores por defecto se definen en los perfiles
# Solo configura si quieres sobrescribir

# Habilitar/deshabilitar limpieza automática de tokens
# TOKEN_CLEANUP_ENABLED=true

# Cron para ejecutar la limpieza
# Dev: cada 30 minutos
# Prod: cada 6 horas
# TOKEN_CLEANUP_CRON=0 */30 * * * *

# Días para mantener tokens revocados
# Dev: 30 días
# Prod: 7 días
# REVOKED_TOKEN_RETENTION_DAYS=30

# ============================
# LOGGING
# ============================
# Los valores por defecto se definen en los perfiles
# Solo configura si quieres sobrescribir

# Nivel de log general de la aplicación
# Valores: TRACE, DEBUG, INFO, WARN, ERROR
# APP_LOG_LEVEL=DEBUG

# Nivel de log para autenticación
# AUTH_LOG_LEVEL=DEBUG

# Nivel de log para seguridad
# SECURITY_LOG_LEVEL=DEBUG

# Nivel de log para Spring Security
# SPRING_SECURITY_LOG_LEVEL=DEBUG

# Nivel de log para SQL de Hibernate
# SQL_LOG_LEVEL=DEBUG

# ============================
# CORS
# ============================
# Los valores por defecto se definen en los perfiles
# Dev: http://localhost:3000,http://localhost:5173
# Prod: debe configurarse la URL de producción
CORS_ALLOWED_ORIGINS=http://localhost:3000

# ============================
# SERVER
# ============================
SERVER_PORT=8080

# ============================
# EMAIL (Opcional)
# ============================
EMAIL_PROVIDER=azure
MAIL_USERNAME=
MAIL_PASSWORD=
TEST_EMAIL=test@example.com

# ============================
# AZURE (Opcional)
# ============================
AZURE_COMMUNICATION_CONNECTION_STRING=
AZURE_SENDER_ADDRESS=DoNotReply@domain.com
AZURE_STORAGE_CONNECTION_STRING=

# ============================
# STORAGE
# ============================
STORAGE_CONTAINER_NAME=uploads
STORAGE_MAX_FILE_SIZE=5242880
STORAGE_MAX_FILES_COUNT=5
STORAGE_MAX_TOTAL_SIZE=20971520
STORAGE_ALLOWED_CONTENT_TYPES=image/jpeg,image/png,image/webp

# ============================
# REDIS (Opcional - para cache de tokens)
# ============================
REDIS_HOST=localhost
REDIS_PORT=6379

