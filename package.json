{
  "name": "commerce-platform",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "storefront:dev": "npm --workspace @commerce/storefront run dev",
    "storefront:build": "npm --workspace @commerce/storefront run build"
  },
  "overrides": {
    "postcss": "8.5.26"
  }
}
